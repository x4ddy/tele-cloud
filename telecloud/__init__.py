"""TeleCloud — Telegram-backed cloud storage.

Top-level package. Each subpackage is a module per SPEC.md §6 and is built in its
own session. This file only marks ``telecloud`` as a package so that
``telecloud.config`` (and future modules) are importable.
"""
